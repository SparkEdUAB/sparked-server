scalar Date
type Query {
  getUnits: [Unit]
}
type Mutation {
  addUnit(
    name: String!
    courseId: String!
    createdBy: String
    createdAt: Date
  ): Unit
}
type Unit {
  _id: String
  name: String
  courseId: String
  topics: [Topic]
  createdAt: Date
  createdBy: String
}
