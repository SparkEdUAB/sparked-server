scalar Date
type Query {
  getUnits: [Unit]
}
type Mutation {
  addUnit(name: String!, createdBy: String, createdAt: Date): Unit
}
type Unit {
  _id: String
  name: String
  createdAt: Date
  createdBy: String
}
